<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.14</version>
		<relativePath/>
	</parent>

	<groupId>com.x</groupId>
	<artifactId>x-redis</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>jar</packaging>
	<name>x-redis</name>
	<description>Redis cache library for X platform services</description>
	<url>https://github.com/VathanaProgrammer/x-redis</url>

	<scm>
		<connection>scm:git:https://github.com/VathanaProgrammer/x-redis.git</connection>
		<developerConnection>scm:git:https://github.com/VathanaProgrammer/x-redis.git</developerConnection>
		<url>https://github.com/VathanaProgrammer/x-redis</url>
	</scm>

	<!-- Publish: mvn -s maven/settings-github.xml deploy  (or scripts/deploy-shared-libs.sh) -->
	<distributionManagement>
		<repository>
			<id>github-x-redis</id>
			<name>GitHub Packages</name>
			<url>https://maven.pkg.github.com/VathanaProgrammer/x-redis</url>
		</repository>
		<snapshotRepository>
			<id>github-x-redis</id>
			<name>GitHub Packages Snapshots</name>
			<url>https://maven.pkg.github.com/VathanaProgrammer/x-redis</url>
		</snapshotRepository>
	</distributionManagement>

	<properties>
		<java.version>17</java.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-redis</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-cache</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-autoconfigure</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-configuration-processor</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.datatype</groupId>
			<artifactId>jackson-datatype-jsr310</artifactId>
		</dependency>
		<!-- Serializes uninitialized lazy JPA associations as null (safe for Redis cache) -->
		<dependency>
			<groupId>com.fasterxml.jackson.datatype</groupId>
			<artifactId>jackson-datatype-hibernate6</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!-- Library module — do not repackage as an executable jar -->
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
